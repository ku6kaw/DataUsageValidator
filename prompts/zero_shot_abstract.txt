### 指示
以下の「被引用論文」のタイトルとアブストラクトを読んで、これが「引用元データ論文」で提示されたデータセットを実際に使用しているかどうかを判断してください。
指定されたデータ論文のデータセットを「実際に使用している」場合は "Used"、そうでない場合は "Not Used" とだけ、JSON形式で回答してください。

### 入力データ
- 引用元データ論文のタイトル: {cited_data_paper_title}
- 被引用論文のタイトル: {citing_paper_title}
- 被引用論文のアブストラクト: {citing_paper_abstract}

### 出力
```json
{{
  "decision": "Used" or "Not Used"
}}